name: Rust build

on:
  push:

env:
  CARGO_TERM_COLOR: always

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout
        uses: actions/checkout@v4
        with:
          submodules: true

      - name: Cache build
        uses: Swatinem/rust-cache@v2
        with:
          workspaces: |
            blus-mini-mk1
            blus-mini-mk2
            blackpill-pcm5102a

      - name: Build
        run: bash .github/ci/build.sh

      - name: Release
        uses: softprops/action-gh-release@v2
        if: startsWith(github.ref, 'refs/tags/')
        with:
          files: |
            blackpill-pcm5102a/target/thumbv7em-none-eabihf/release/blackpill-pcm5102a
            blus-mini-mk2/target/thumbv7em-none-eabihf/release/blus-mini-mk2
